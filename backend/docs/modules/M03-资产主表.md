# M03 — 资产主表 CRUD + 状态机

## 职责

资产全生命周期管理：新增、查询、编辑、报废，以及核心状态机流转。
这是整个系统的数据中心，M04/M05 的状态变更最终写回此表。

## 涉及的表

| 表 | 操作 |
|---|---|
| `asset` | 主表，全量读写 |
| `asset_allocation` | 每次状态变更同步更新持有关系 |
| `asset_log` | 每次状态变更自动写入（service 层内置，非独立模块） |

## 状态机

```
[新增] → IDLE（闲置）
IDLE   → IN_USE          （领用，由 M04 触发）
IDLE   → DISCARD         （直接报废）
IN_USE → IDLE            （归还，由 M04 触发）
IN_USE → DISCARD         （在用中报废）
IDLE / IN_USE → PENDING_CONFIRM （发起领用申请后，由 M04 触发）
PENDING_CONFIRM → IN_USE （审批通过，由 M04 触发）
PENDING_CONFIRM → IDLE   （审批拒绝，由 M04 触发）
```

状态变更除"新增"外，均不由 AssetService 自行触发——M04/M05 的 service 调 AssetService 提供的
`changeStatus(assetId, newStatus, operatorId, note)` 方法，该方法同时写 asset_log。

## 对外 API

```
GET    /api/v1/assets                     # 分页列表，支持多条件筛选
GET    /api/v1/assets/{id}                # 详情
POST   /api/v1/assets                     # 新增（编码服务端自动生成，状态自动设为 IDLE）
PUT    /api/v1/assets/{id}                # 编辑基础信息（不含状态变更；barcode 可选——不传保持原编码）
POST   /api/v1/assets/{id}/discard        # 报废
GET    /api/v1/assets/{id}/logs           # 该资产的操作日志
GET    /api/v1/assets/export              # 导出 Excel（对齐现有 AssetCard 格式）
POST   /api/v1/assets/import              # 批量导入（历史数据迁移入口）
```

### 资产编码自动生成（2026-08-21 实施决策）

新增资产编码由服务端生成，客户端传入 `barcode` 即忽略（不报错，兼容旧前端）：

- **格式**：`{分类前缀}-{yyyyMMdd}-{4位序号}`，如 `SKSCDM-20260821-0001`
- **前缀**：取 `asset_category.barcode_prefix`（V20260824 迁移加列并按旧系统约定种子：
  SKSCDM 镀膜 / SKSCFZ 辅助 / SKSCJC 检测 / SKBGDN IT数码，见 M08 迁移文档）；
  未配置前缀或未选分类时回退 `SK`
- **序号**：与 ARE/BOR 单号同模式取号（likeRight + orderByDesc + LIMIT 1 FOR UPDATE
  锁定读串行，uk_asset_barcode 兜底），当日按前缀各自从 0001 起
- **M08 隔离**：日期段使新编码与旧系统编码（`前缀-序号`，无日期段）命名空间隔离，
  历史迁移 upsert 按 barcode 不冲突
- **编辑语义**：PUT 不传 barcode → 保持原编码；传则改码（唯一校验照旧，用于旧数据纠错场景）

### 报废联动闭环持有关系（2026-08-21 修复）

**问题**：在用资产直接报废（IN_USE→DISCARD 合法流转）后，asset_allocation 无人闭环，
持有关系悬死——退库入口因 DISCARD→IDLE 非法被 409 卡死，前端领用&退库页永远显示"持有中"
（验收发现：SFBGIT2528 / ARE202608210001）。参考系统 Ciyo 的 scrap 同样不闭环分配，
且退库会把已报废资产静默改回闲置，属参考系统要规避的缺陷。

**修复**：`changeStatus` 落 DISCARD 时联动——

1. 该资产持有中（returned_at 为空）的 asset_allocation 全部终结：
   `returned_at=now`，note 记"资产报废，持有关系随报废终结：{报废原因}"
2. 清空资产 `user_id`/`user_department`（报废为终态，无持有人）
3. 报废日志追加"持有人【张三】的持有关系随报废终结"（审计可追溯原持有人）

PENDING_CONFIRM→DISCARD 已被状态机阻止（409），待审批单据不存在悬死路径。
退库接口的 DISCARD→IDLE 409 保持不变（报废资产不可复活，防御并发竞态）。

## 关键查询条件（对齐 Excel 中实际使用的过滤维度）

`status / categoryId / locationId / companyId / userId / adminUserId / keyword（barcode/name/sn）`

## 依赖

- M01（鉴权骨架）、M02（category/location/model/manufacturer/supplier 已有数据）

## 完成标准

```
mvn clean verify 通过
POST /api/v1/assets → code 200（统一 Result 契约），响应含服务端生成的 barcode（分类前缀-日期-序号）
GET  /api/v1/assets?status=IDLE&categoryId=1 → 正确过滤
POST /api/v1/assets/{id}/discard → 状态变 DISCARD，asset_log 有记录
PUT  /api/v1/assets/{id}/status 直接调用 → 405（状态只能通过业务接口变更）
```

## 单次 AI 会话切分建议

**会话 M03**（单会话可完成）：entity/dto/mapper/service/controller + 状态机方法
`changeStatus()` + 分页查询 + 报废接口 + 单测（状态流转覆盖所有合法路径）

## 接手前必读

`AGENTS.md` → `STATUS.md` → `docs/adr/0006-数据库核心表设计.md` → `docs/modules/M01-骨架与鉴权.md`
