# M03 资产主表前端验收清单

> 验收日期：2026-08-21　分支：`feat/m02-basedata`（worktree）
> 契约依据：asset-backend `docs/modules/M03-资产主表.md`（后端 M03 已完成）
> 范围说明：M04 领用/借用页面因计划编排错误提前完成，已 stash 保留（`stash@{0}`），待 M04 后端就绪时恢复

## 功能验收

### 资产列表（/assets）

- [x] GET /api/v1/assets 服务端分页（page/size，默认 20 条/页，浏览器验证请求带参）
- [x] 状态 tabs（全部/在用/闲置/维修/报废）→ status 参数服务端过滤（浏览器验证）
- [x] 搜索防抖 300ms → keyword 参数（资产编码/名称/序列号模糊匹配，浏览器验证）
- [x] 深链同步：`?status=using&page=2` 直接访问正确加载（浏览器验证 + 截图）
- [x] `vue-tsc --noEmit` 零错误通过

### 资产新增/编辑弹窗（AssetModal）

- [x] 表单校验：name 必填；barcode 新增不录入（服务端自动生成"分类前缀-日期-序号"，提交后回显）、编辑必填可改码（curl 验证：无分类生成 SK-、带分类生成 SKBGDN-、编辑不传保持原码/传则改码）
- [x] 基础数据联动下拉：分类（树形）/位置（树形）/厂商/供应商/公司（浏览器验证选项加载）
- [x] 新增固定状态 IDLE，状态不开放编辑（契约：状态只能走业务端点流转）
- [x] POST /api/v1/assets → 200，created_at/updated_at 自动填充（curl 验证）
- [x] PUT /api/v1/assets/{id} → 200，updatedAt 刷新（curl 验证）

### 资产详情抽屉（AssetDetailDrawer）

- [x] 全字段展示（含关联名称：分类/位置/归属公司等，浏览器验证 + 截图）
- [x] 操作日志时间线：GET /api/v1/assets/{id}/logs（浏览器验证"新增"记录）
- [x] 报废：POST /api/v1/assets/{id}/discard → 200，日志追加
      `【状态】由【闲置】变更为【报废】`（curl 验证）
- [x] 报废按钮仅对可报废状态（闲置/在用）开放（DISCARDABLE_STATUSES 状态机约束）

### 导出

- [x] GET /api/v1/assets/export → 真实 Excel（OOXML，4153 字节，curl 验证）
- [x] axios `responseType: 'blob'` 支持 + content-type 判断（JSON-in-blob 错误转 ElMessage）

### 导航集成

- [x] 侧边栏"资产管理"入口 + 路由 /assets（DefaultLayout 图标高亮）
- [x] 路由 name `assets-list` 与 keep-alive 多页签缓存键一致

### M02.5 契约变更适配（厂商/供应商服务端分页）

- [x] GET /v1/manufacturers、/v1/suppliers 切换 PageResp 分页契约（type-check + 回归验证）
- [x] 状态 tabs（全部/启用/停用）+ keyword 服务端搜索 + 分页控件（浏览器回归验证）

## 后端全链路 curl 验证（经 Vite 代理 5173 → asset-backend 6006）

| # | 端点 | 结果 |
|---|---|---|
| 1 | GET /api/v1/assets?status=IDLE | 200，status 过滤正确 |
| 2 | POST /api/v1/assets | 200，状态自动 IDLE |
| 3 | PUT /api/v1/assets/2 | 200，amount 100→200 |
| 4 | GET /api/v1/assets/2/logs | 200，"新增"记录含操作人 |
| 5 | POST /api/v1/assets/2/discard | 200 |
| 6 | GET /api/v1/assets/export | 200，application/vnd...sheet + OOXML |
| 7 | 报废后日志 | 追加状态变更记录 |
| 8 | POST 重复 barcode | 409 唯一约束生效 |

（验证账号：assetfe，asset-资产管理员角色；验证数据 M03-VERIFY-001 已清理）

## 已知测试环境限制

TRAE 浏览器自动化工具仍会拦截带 body 的 POST（同 M02），弹窗内表单提交的端到端点击
未自动化；已按 M02 先例用 curl 经 Vite 代理交叉验证全链路 200。

**待用户在真实浏览器手动验收**：新增资产弹窗填写提交 → 弹窗关闭 + 列表刷新 + 成功提示；
详情抽屉内"报废"按钮确认提交 → 状态变更为报废。

## 范围外（如实记录）

- POST /api/v1/assets/import 批量导入（历史数据迁移入口）：后端已就绪，前端 UI 未做，
  待历史数据迁移需求明确时补充
- M04 领用/借用触发的状态流转（PENDING_CONFIRM 等）：属 M04 范围，页面已 stash 保留
